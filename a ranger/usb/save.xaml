<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="USB Manager" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#000000">

    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="USB Keys"
                   Foreground="White"
                   FontSize="22"
                   Grid.ColumnSpan="2"
                   Margin="0,0,0,10"/>

        <!-- LISTE USB -->
        <ScrollViewer Grid.Row="1" Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <ItemsControl Name="UsbList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#444" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding IdDisplay}" Foreground="#CCC" FontSize="12"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}" FontSize="13"/>
                                    <TextBlock Text="{Binding DriveDisplay}" Foreground="#DDD" FontSize="12"/>
                                </StackPanel>

                                <Button Grid.Column="2"
                                        Content="Eject"
                                        Padding="10,5"
                                        Margin="10,0,0,0"
                                        Tag="{Binding DriveLetter}"
                                        Background="#AA3333"
                                        Foreground="White"
                                        IsEnabled="{Binding CanEject}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- PANNEAU DE DÉTAILS -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="#1E1E1E"
                BorderBrush="#333"
                BorderThickness="1"
                CornerRadius="6"
                Padding="15"
                Margin="15,0,0,0">

            <StackPanel>

                <TextBlock Text="Détails de la clé"
                           FontSize="18"
                           Foreground="White"
                           Margin="0,0,0,10"/>

                <StackPanel Margin="0,5">
                    <TextBlock Text="Nom" Foreground="#888"/>
                    <TextBlock Text="{Binding SelectedItem.Name, ElementName=UsbList}"
                               Foreground="White" FontSize="14"/>
                </StackPanel>

                <StackPanel Margin="0,5">
                    <TextBlock Text="Identifiant" Foreground="#888"/>
                    <TextBlock Text="{Binding SelectedItem.IdDisplay, ElementName=UsbList}"
                               Foreground="White" FontSize="14"/>
                </StackPanel>

                <StackPanel Margin="0,5">
                    <TextBlock Text="Lettre" Foreground="#888"/>
                    <TextBlock Text="{Binding SelectedItem.DriveDisplay, ElementName=UsbList}"
                               Foreground="White" FontSize="14"/>
                </StackPanel>

                <StackPanel Margin="0,5">
                    <TextBlock Text="Statut" Foreground="#888"/>
                    <TextBlock Text="{Binding SelectedItem.Status, ElementName=UsbList}"
                               Foreground="{Binding SelectedItem.StatusColor, ElementName=UsbList}"
                               FontSize="14"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <Button Content="Ouvrir" Width="80" Margin="0,0,10,0"
                            Background="#0078D4" Foreground="White" Name="OpenButton"/>

                    <Button Content="Copier" Width="80" Margin="0,0,10,0"
                            Background="#444" Foreground="White" Name="CopyButton"/>

                    <Button Content="Éjecter" Width="80"
                            Background="#AA3333" Foreground="White" Name="EjectButton"/>
                </StackPanel>

            </StackPanel>
        </Border>

        <!-- BAS -->
        <Grid Grid.Row="2" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Name="RefreshButton"
                    Content="Refresh" Width="120" Height="28"
                    Background="#3366AA" Foreground="White" Margin="0,5,10,0"/>

            <Button Grid.Column="1" Name="EjectAllButton"
                    Content="Eject All" Width="120" Height="28"
                    Background="#AA3333" Foreground="White" Margin="0,5,10,0"/>

            <TextBlock Grid.Column="2" Name="FooterText"
                       Foreground="#888" FontSize="11"
                       HorizontalAlignment="Right" VerticalAlignment="Center"
                       Margin="0,5,0,0"/>
        </Grid>

    </Grid>
</Window>

